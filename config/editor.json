{
    // useful for editing non-python files
    "never_execute_code":1,

    // used if a windows system is not available
    "console_editor":"nano",

    // used if running inside xwindows
    "editor":"emacsclient -n"
}
